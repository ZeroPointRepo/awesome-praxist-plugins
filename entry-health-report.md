Checked **3** linked repositories via the GitHub API.

These are problems a link checker cannot see: a renamed or archived repository still returns HTTP 200, so the weekly link sweep passes it clean.

**No renamed, archived or deleted entries found.**
---
*Automated weekly entry-health check. Advisory only, never blocks a pull request.*
